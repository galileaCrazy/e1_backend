# Base de datos
DB_NAME=MVPClinica
DB_USER=postgres
DB_PASSWORD=tu_password_postgres

# JWT — mismo secret que usa Spring Boot (base64, mínimo 32 chars)
JWT_SECRET=tu_clave_secreta_base64

# IA — obtén tu clave en https://console.groq.com
GROQ_API_KEY=tu_groq_api_key
